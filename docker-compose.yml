services:
  mariadb:
    image: mariadb:11
    container_name: mapadinternational-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpassword}
      MYSQL_DATABASE: ${DB_DATABASE:-mapadinternational}
      MYSQL_USER: ${DB_USER:-mapadinternational}
      MYSQL_PASSWORD: ${DB_PASSWORD:-mapadinternational}
    ports:
      - "${DB_PORT:-3306}:3306"
    volumes:
      - mariadb_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mariadb_data:
